"""Vendor SDK adapters belong here.

For example, ``gemini_dialogue.py`` may implement ``DialogueProvider`` while
keeping Gemini imports and request/response translation outside System 1 domain
modules. Register each adapter factory in the composition root.
"""
