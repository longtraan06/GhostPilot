# Local GhostPilot development environment, including microphone VAD debug UI.
# Project source is installed editable so `python -m ghostpilot...` resolves normally.
-e .
numpy>=1.26,<3.0
sounddevice>=0.4.6,<1.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
